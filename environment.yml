name: micro-flash-attn
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy>=1.24.0
  - pandas>=2.0.0
  - matplotlib>=3.7.0
  - pytest>=7.4.0
  - pip:
      - torch>=2.2.0
      - triton>=2.2.0